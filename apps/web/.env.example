# Database
DATABASE_URL="postgresql://portfolio:portfolio@localhost:5432/portfolio"

# Session (iron-session — mínimo 32 caracteres)
SESSION_SECRET="troque_por_uma_string_aleatoria_de_pelo_menos_32_chars"

# Admin criado via POST /api/auth/setup (apenas uma vez, quando nenhum usuário existe)
